import { Styles } from '@chakra-ui/theme-tools'

const styles: Styles = {
  global: () => ({
    html: {
      scrollBehavior: 'smooth',
      body: {
        bg: 'white',
      },
    },
  }),
}

export default styles
